#!/bin/bash

if [ $# -eq 0 ]; then
    alacritty --title Terminal
elif [[ $1 = -* ]]; then
    alacritty --title Terminal "$@"
elif [[ -d "$1" ]]; then
    alacritty --title Terminal --working-directory "$1"
else
    alacritty --title Terminal --command "$@"
fi
